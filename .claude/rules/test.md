# test

- `describe` / `it` のメッセージは日本語で記述する
- カバレッジ75%を目指す
- ブラックボックステストとホワイトボックステストを使い分ける
  - component(`{機能名}.test.ts(x)`): ブラックボックステスト。公開された props / DOM を通して振る舞いを検証し、内部実装(state 変数名など)には依存しない
  - hook(`use{機能名}.test.ts(x)`): ホワイトボックステスト。内部状態やロジックの分岐を直接検証し、カバレッジを担保する
